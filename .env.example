# Optional — enables accounts + cross-device plan sync.
# Without these, FitFlow runs fully local (plans stored in the browser).
# Get both values from: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
